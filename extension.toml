id = "doctest-runner"
name = "Doctest Runner"
description = "Run C++ doctest test cases with one click"
version = "0.1.0"
schema_version = 1
authors = ["Anders Wind"]
repository = "https://github.com/anders-wind/zed-doctest-runner"
